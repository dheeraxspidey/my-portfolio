import React from 'react';
import ScrollBasedSectionTransition from '../ui/ScrollBasedSectionTransition';

const ScrollBasedPortfolio = () => {
  return (
    <div className="min-h-screen bg-gradient-to-b from-gray-900 via-gray-800 to-gray-900">
      <ScrollBasedSectionTransition />
    </div>
  );
};

export default ScrollBasedPortfolio;
